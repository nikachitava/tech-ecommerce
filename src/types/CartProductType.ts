export interface CartProductType {
    id: string;
    name: string;
    thumbnail: string;
    price: number;
    quantity: number;
}

export interface WishProductType {
    id: string;
    name: string;
    thumbnail: string;
    price: number;
}